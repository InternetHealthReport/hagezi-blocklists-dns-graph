[project]
name = "hagezi-blocklists-intel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dnspython>=2.6.1",
    "requests>=2.32.0",
    "uvloop>=0.20.0; sys_platform != 'win32'",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "responses>=0.25.0",
]
